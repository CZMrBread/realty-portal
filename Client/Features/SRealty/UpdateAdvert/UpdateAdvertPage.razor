@page "/srealty/{Id:guid}/edit"
@layout AgencyLayout

<PageTitle>Update Advert</PageTitle>

<div class="container mt-4">
    <h1 class="h3 mb-4">Úprava inzerátu</h1>

    @* TODO: load the advert from srealty/{Id} and PUT it back once the client API is written *@
    <AdvertForm Model="advert" SubmitLabel="Uložit změny"/>
</div>

@code {
    private readonly SrealityAdvertDto advert = new();

    [Parameter] public Guid Id { get; set; }
}
